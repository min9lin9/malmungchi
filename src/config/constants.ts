export const CORPUS_SCHEMA_VERSION = 1;

export const DEFAULT_SEARCH_LIMIT = 10;
export const MAX_SEARCH_LIMIT = 50;
export const DEFAULT_EPISODE_SECTION = "metadata";

export const DATE_REGEX = /^\d{4}-\d{2}-\d{2}$/;
